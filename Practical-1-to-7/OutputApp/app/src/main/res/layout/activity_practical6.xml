<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_practical6.xml – Practical 6: Intents, Events, Adapters Demo
    Contains:
      • Button to launch an implicit intent (open website in browser)
      • RecyclerView populated by ItemAdapter
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/screen_padding">

    <!-- Large title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Practical 6 Output"
        android:textAppearance="?attr/textAppearanceHeadline5"
        android:gravity="center"
        android:paddingBottom="16dp" />

    <!-- Implicit Intent section -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Implicit Intent"
        android:textAppearance="?attr/textAppearanceSubtitle1"
        android:textColor="@color/primary"
        android:paddingBottom="4dp" />

    <Button
        android:id="@+id/btnOpenWebsite"
        style="@style/Widget.Material3.Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Open developer.android.com"
        android:layout_marginBottom="16dp" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/divider"
        android:layout_marginBottom="12dp" />

    <!-- RecyclerView section -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="RecyclerView + Adapter (tap an item)"
        android:textAppearance="?attr/textAppearanceSubtitle1"
        android:textColor="@color/primary"
        android:paddingBottom="8dp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="8dp" />

</LinearLayout>
